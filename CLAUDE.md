# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
# Build
cargo build

# Run all tests
cargo test

# Run a single test by name
cargo test <test_name>

# Build WASI component (requires cargo-component tool)
cargo component build
```

## Architecture

Pure Rust NFSv3 client library (NetApp). Compiles to both `rlib` and `cdylib` (WASI component). Only NFSv3 is implemented; NFSv4/4.1/4.2 version strings are accepted in URLs but return `Unsupported`.

### Entry Point

`parse_url_and_mount(url, max_retries)` in `src/lib.rs` parses a `nfs://host/export[?params]` URL and returns a `Box<dyn Mount>`. It calls `nfs3::mount()` for NFSv3 (the only real implementation).

URL query params: `uid`, `gid`, `version`, `nfsport`, `mountport`, `readdir-buffer`, `rsize`, `wsize`.

### Public Interface: `Mount` Trait (`src/mount.rs`)

Async trait (via `async-trait`) defining all NFS operations. Version-agnostic. Each operation has two forms:
- `op(fh, ...)` — takes a raw file handle (`bytes::Bytes`)
- `op_path(path, ...)` — resolves path via LOOKUP then calls the fh variant

### NFSv3 Implementation (`src/nfs3/`)

Two-layer internal design:

**`nfs3::Mount` struct** (`src/nfs3/mod.rs`) — inner struct holding the RPC client, auth, root fh, and transfer size limits. Contains low-level `_method()` calls generated by the `nfs3_call!` macro:

```rust
nfs3_call!(method_name, ProcedureName, ArgsType, ResType)
// Expands to: pack XDR args → rpc.call() → unpack XDR response
```

**`nfs3::Mount3` struct** (`src/nfs3/mount.rs`) — wraps `nfs3::Mount` and implements the public `crate::Mount` trait. Contains the mount flow logic and delegates all operations to `self.m`.

Each NFSv3 procedure (access, read, write, lookup, etc.) has its own file in `src/nfs3/` implementing public higher-level methods on `nfs3::Mount` (path resolution, chunking, etc.).

**Mount flow** (`src/nfs3/mount.rs::mount_on_addr`):
1. Connect TCP to NFS port (from portmapper or URL)
2. Connect TCP to MOUNT port (separate connection if different port)
3. MOUNT NULL (ping)
4. MOUNT MNT → get root file handle
5. NFS NULL (ping)
6. FSINFO → clamp `rsize`/`wsize` to server limits (min 8 KiB, max 4 MiB)

### XDR Types

`src/nfs3/nfs3xdr.rs` and `src/nfs3/mount3xdr.rs` are **generated code** from XDR spec files by `xdrgen`. **Do not edit directly.**

### RPC Layer (`src/rpc/`)

**`rpc::Client`** holds two stream IDs: `nfs_stream_id` (always used) and optional `mount_stream_id` (used only for MOUNT program calls when MOUNT runs on a different port than NFS).

`Client::call()` implements retry-with-reconnect: on `BrokenPipe`/`ConnectionAborted`/`ConnectionReset`, sleeps `num_retries * 100ms` and reconnects, up to `max_retries` (default 10) attempts.

**TCP stream management** (`src/lib.rs`): Global `LazyLock<HashMap<u32, Arc<RwLock<TcpStream>>>>` keyed by random `u32` IDs. Source port is bound to 500–999 range (random, retried up to 100 times).

**Auth** (`src/rpc/auth.rs`): Only `AUTH_NULL` and `AUTH_UNIX` are used. `AUTH_UNIX` encodes uid/gid from the URL (or system `getuid()`/`getgid()` on Unix, 65534 on Windows).

### Known Limitations

**Stale file handles after server reboot**: TCP reconnection (`StreamMux::reconnect`) restores the network connection but does **not** re-mount. If the NFS server reboots, all previously obtained file handles become stale (`NFS3ERR_STALE`). The `_path` methods will also fail because intermediate lookup handles are stale. Callers should catch `NfsError::Nfs3` with `NFS3ERR_STALE` and re-mount (`parse_url_and_mount`) to obtain fresh handles. This is consistent with how libnfs and other NFS client libraries handle this scenario.

### WASI Component (`src/bindings.rs`, `src/component.rs`)

`src/bindings.rs` — **auto-generated** by `wit-bindgen` from `wit/`. Do not edit.

`src/component.rs` — hand-written bridge between WIT types and Rust types. Maps WIT types (prefixed `Wit*`) to/from native types using `From` impls.

All WASI-specific code is gated with `#[cfg(target_os = "wasi")]`. The WASI build uses a separate `MOUNTS` global HashMap (keyed by `u32`) to store active mounts as resources. The `wit/` directory contains the WASI Interface Types spec and WASI dependency stubs.

### Code Style

- 单 crate 项目，公共类型通过 `src/lib.rs` 的 `pub use` 重导出

### use 语句规范（强制）

**约束 1 — 位置：** 所有 `use` 语句必须集中在文件顶部，禁止在函数体、`impl` 块或任何嵌套作用域内声明 `use`。

- 唯一例外：`#[cfg(test)]` / `tests/` 块内可在块内部使用 `use`

**约束 2 — 路径深度：** 在函数体/表达式中直接书写的路径尽量不超过三层。超长路径应通过顶部 `use` 导入后使用短名称。

```rust
// ❌ 禁止 — 过深路径
let fh = nfs3::fastxdr::nfs_generated::xdr::nfs_fh3::new();

// ✅ 正确 — 通过顶部 use 导入
use super::nfs_fh3;
```

> 例外：`crate::mount::ReaddirEntry`、`crate::Mount` 等本 crate 内常用的跨模块路径可以保留，但鼓励在频繁使用时通过 `use` 导入简化。`std::io::Error` 等标准库路径亦可酌情保留。

### 重构规范（强制）

**重构只改结构，不改语义。重构前后的可观测行为必须完全一致。**

- 允许：提取函数、重命名变量、调整模块结构、消除重复、改善类型签名
- 禁止：在重构的同时修改逻辑、添加新功能、改变错误处理路径、调整并发行为

**验证方式：** 重构完成后，所有现有测试必须 pass，且不得修改测试本身来迁就重构。

```rust
// ❌ 重构时偷偷改了逻辑（原来出错时跳过，现在改成返回错误）
// before
if let Err(e) = do_thing() { error!("{}", e); }
// after（声称是"重构"，实为改变语义）
do_thing()?;

// ✅ 保留原有语义
if let Err(e) = do_thing() { error!("{}", e); }
```

如果在重构过程中发现原有逻辑有问题，应**单独提交**修复，不与重构混在一起。

### 错误处理规范（强制）

#### 1. 禁止 `.unwrap()` / `.expect()`

**严禁在任何生产代码中使用** **`.unwrap()`** **和** **`.expect()`。**

这两个方法在值为 `None` / `Err` 时会直接 panic，导致整个进程崩溃，在高吞吐服务中不可接受。

```rust
// ❌ 禁止
let val = some_option.unwrap();
let val = some_result.expect("should not fail");

// ✅ 用 ? 传播错误（在返回 Result 的函数中）
let val = some_result?;

// ✅ 用 if let / match 处理 Option
if let Some(val) = some_option { /* use val */ }

// ✅ 用 ok_or / ok_or_else 将 Option 转为 Result 后传播
let val = some_option.ok_or(MyError::NotFound)?;

// ✅ 提供合理默认值
let val = some_option.unwrap_or_default();
```

**唯一例外：** `#[cfg(test)]` 块或 `tests/` 目录下的测试代码。

验证命令（提交前检查）：

```bash
grep -rn '\.unwrap()\|\.expect(' --include="*.rs" \
  $(find . -name "*.rs" -not -path "*/tests/*" -not -path "*_test.rs")
```

***

#### 2. 错误类型：优先用 `thiserror` 定义结构化错误

**不得使用** **`Box<dyn Error>`** **或** **`String`** **作为跨边界错误类型。** 应使用 `thiserror` 定义具有明确语义的错误枚举。

```rust
use thiserror::Error;

#[derive(Error, Debug)]
pub enum NfsError {
    #[error("IO error: {0}")]
    Io(#[from] std::io::Error),

    #[error("NFS3 error: {0}")]
    Nfs3(ErrorCode),

    #[error("Mount error: {0}")]
    Mount(MountErrorCode),

    #[error("RPC error: {0}")]
    Rpc(String),
}

pub type Result<T> = std::result::Result<T, NfsError>;
```

**不得**用 `.to_string()` 丢失类型信息后再包进 `String` 变种。

> 注：当前代码仍使用 `std::io::Error` + `ErrorKind::Other` 包装所有错误。上述是目标方向，迁移可渐进完成。


## High-Performance IO / Async Patterns

**必须遵守的规则：**

- 数据传输用 `Bytes`/`BytesMut` 零拷贝，**禁止** `Vec<u8>` clone（RPC 响应、NFS read/write 数据）
- 共享计数用 `AtomicU32`/`AtomicU64`，**禁止** `Mutex<u64>`
- CPU 密集任务用 `spawn_blocking`，**禁止**在 async fn 中阻塞
- channel 背压靠 `bounded()` 自然限流，**禁止** unbounded channel
- async 上下文中的锁优先使用 `tokio::sync::Mutex`，避免 `std::sync::Mutex` 阻塞 tokio 工作线程（短临界区例外）
