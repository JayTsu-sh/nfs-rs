name: Release validation

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Commit or tag to validate
        required: true
        default: main

permissions:
  contents: read

concurrency:
  group: terrasync-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: [self-hosted, linux, x64, terrasync-lab, nfs-rs]
    environment: release
    timeout-minutes: 240
    env:
      RUN_ID: release-${{ github.run_id }}-${{ github.run_attempt }}
      NFS_RS_LAB_V40_EVIDENCE_DIR: ${{ runner.temp }}/nfsv40-evidence-records
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref }}
      - name: Discover persisted Rust toolchain
        run: |
          toolchain_bin=""
          for candidate in \
            "${RUSTUP_HOME:-$HOME/.rustup}/toolchains/1.95.0-x86_64-unknown-linux-gnu/bin" \
            "/home/github-runner/.rustup/toolchains/1.95.0-x86_64-unknown-linux-gnu/bin" \
            "/opt/rust/1.95.0/bin"; do
            if [[ -x "$candidate/rustc" && -x "$candidate/cargo" ]]; then
              toolchain_bin="$candidate"
              break
            fi
          done
          if [[ -z "$toolchain_bin" ]]; then
            echo "complete pre-provisioned Rust 1.95.0 toolchain not found" >&2
            exit 1
          fi
          echo "$toolchain_bin" >> "$GITHUB_PATH"
          echo "using pre-provisioned toolchain at $toolchain_bin"
      - name: Use pre-provisioned Rust 1.95.0
        run: |
          rustc --version | grep -q '^rustc 1\.95\.0 '
          rustc --version --verbose
          cargo --version
      - uses: Swatinem/rust-cache@v2
        with:
          cache-bin: false
      - run: tests/lab/health-check.sh
      - run: tests/lab/prepare-run.sh "$RUN_ID"
      - run: flock --wait 10800 /tmp/terrasync-lab-tests.lock cargo test --all-targets --locked --no-fail-fast
      - run: flock --wait 10800 /tmp/terrasync-lab-tests.lock tests/lab/run-e2e.sh "$RUN_ID"
      - name: Record NetApp NFSv4.0 semantic evidence
        run: |
          evidence_dir="$NFS_RS_LAB_V40_EVIDENCE_DIR"
          tests/lab/record-nfsv40-evidence.sh "$evidence_dir" semantic flock --wait 10800 /tmp/terrasync-lab-tests.lock tests/lab/run-netapp-v40-e2e.sh
      - name: Record NetApp NFSv4.0 callback-fault evidence
        run: |
          evidence_dir="$NFS_RS_LAB_V40_EVIDENCE_DIR"
          tests/lab/record-nfsv40-evidence.sh "$evidence_dir" callback-fault flock --wait 10800 /tmp/terrasync-lab-tests.lock tests/lab/run-netapp-v40-callback-fault-e2e.sh "$RUN_ID"
      - name: Record NetApp NFSv4.0 lease-fault evidence
        run: |
          evidence_dir="$NFS_RS_LAB_V40_EVIDENCE_DIR"
          tests/lab/record-nfsv40-evidence.sh "$evidence_dir" lease-fault flock --wait 10800 /tmp/terrasync-lab-tests.lock tests/lab/run-netapp-v40-lease-fault-e2e.sh "$RUN_ID"
      - run: cargo build --release --locked
      - run: cargo package --locked
      - run: cargo publish --locked --dry-run
      - name: Record NetApp NFSv4.0 performance evidence
        run: |
          evidence_dir="$NFS_RS_LAB_V40_EVIDENCE_DIR"
          tests/lab/record-nfsv40-evidence.sh "$evidence_dir" performance flock --wait 10800 /tmp/terrasync-lab-tests.lock tests/lab/run-netapp-v40-performance.sh "$RUN_ID"
      - name: Record NetApp NFSv4.0 cleanup evidence
        run: |
          evidence_dir="$NFS_RS_LAB_V40_EVIDENCE_DIR"
          tests/lab/record-nfsv40-evidence.sh "$evidence_dir" cleanup sudo -n /usr/local/sbin/nfsrs-lab-v40-fault restore-any "$RUN_ID"
      - run: tests/lab/collect-nfsv40-release-evidence.sh "$RUN_ID" release-evidence
      - name: Checksums
        run: find target/release -maxdepth 1 -type f -executable -print0 | sort -z | xargs -0r sha256sum | tee release-sha256.txt
      - uses: actions/upload-artifact@v4
        with:
          name: nfs-rs-release-validation-${{ github.run_id }}
          path: |
            release-sha256.txt
            release-evidence
      - name: Cleanup
        if: always()
        run: |
          sudo -n /usr/local/sbin/nfsrs-lab-v40-fault restore-any "$RUN_ID"
          tests/lab/cleanup-run.sh "$RUN_ID"
